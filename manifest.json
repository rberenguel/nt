{
  "manifest_version": 3,
  "name": "NT",
  "version": "0.2",
  "description": "Replaces the default new tab page with a custom one",
  "chrome_url_overrides": {
    "newtab": "index.html"
  },
  "permissions": ["storage"]
}

# Misc Directory

This directory is for locally cloned projects that you want to serve within NT's iframes.

You can `git clone` or download simple PWAs, standalone HTML projects, or other web apps here to use them in your new tab without committing them to the NT repository.

Everything in this folder except this README is git-ignored.
